#!/bin/sh
## SpectrWM bar

disk() {
	free=$(df -h | awk 'NR==2 {print $1" "$3"/"$2" "$5" "$4}')
	echo -e $free
}

vol() {
	vol=$(amixer get Master | awk '/Mono:/ {print $4}' tr -d '[][]' )
	echo -e "VOL: ${vol}"

}

while :; do
	echo "$(disk) | $(vol) | $(ibus engine) |"
	sleep 0.1
done
